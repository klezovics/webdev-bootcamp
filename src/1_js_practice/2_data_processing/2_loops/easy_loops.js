const array = [1, 3, 4, 5, 6, 2];

// 1. Create a loop using for-of
// Purpose: iterate through elements of an array
// for (let arrayElement of array) {
//   console.log("next array element:",arrayElement);
// }

// 2. Create a loop using forEach array method
// Purpose: iterate through elements of an array
// array.forEach(arrayElement => {
//   console.log('next array element:', arrayElement);
// });

const exchange_rate = {
    USD: 1.2,
    EUR: 4.3,
    GBP: 2.11,
};

// 3. Create a loop using for-in
// Purpose: Iterate through object keys
// for(let key in exchange_rate) {
//    console.log(key,":",exchange_rate[key])
// }
